# 2026-03-17 Developer Words

> 记录开发者与 AI 的对话原文（仅开发者侧发言），按话题顺序整理。

---

## 会话持久化

> 现在有一个问题 没有聊天记录存储功能，每次终止之后，再打开相同路径下的项目，记录就没了 现在希望增加这种功能 但是我之前没有做过 coding agent 能不能帮我看看现在主流的方案是什么

> 行 那就使用方案一吧

> 如果有历史记录 在每次打开的时候也需要把历史记录展开吧

> 把这个历史记录的更新到开发日志中 技术选型记得写上

---

## Plan 模式

> 现在好像没有 plan 模式 如果要实现该怎么实现呢

> 可以帮我实现一下

> 不对呀 你不让调用工具 但是你比如 coding 他需要调用各种工具去查看文件信息 才知道怎么规划呀

> 把 plan 模式的实现更新到今天的开发日志中

> 技术选型即实现方式选择需要补进去

> 帮我提交并且更新 readme

> 我发现即使是这样 他不会输出规划 是不是系统提示词还是需要加一下的

> 把 plan 模式的实现更新到今天的开发日志中

> 技术选型即实现方式选择需要补进去

> 帮我提交并且更新 readme

---

## Token 用量展示 & 上下文压缩

> 我现在想加上上下文压缩 不过这个我之前也没做过 可以给我讲一下怎么实现比较好吗

> 还有一个点 我想在展示的时候让用户知道当前的模型的上下文最大长度限制 以及已经占据的，比如我现在这个是 qwen3.5-plus 我去看了一下他的最大上下文长度是 1,000,000

> 现在压缩是不是还没实现

---

## 压缩体验优化

> 在触发消息压缩时 能不能有一个单独的提示 不然用户以为是出问题了 因为压缩需要额外花时间

---

## 历史记录与 LLM 上下文分离

> 还有就是我看了一下存的聊天记录，似乎现在压缩之后聊天记录也随之被清掉了 只保留了最近几条的数据，我理解这个聊天记录是需要单独存储的吧，毕竟每次用户打开使用，应该是需要展示完整的对话记录，只是每次发送给大模型的是之前压缩过的摘要 当然具体我也没有经历实现过 不知道现在主流设计有那些方案呢

> 可以 实现吧

> session-store.ts 23-27 这里的兼容不用 我这个是自己的项目 就按照当前格式来 把兼容代码去掉吧

> 我想了一下为什么需要两个数组呢 这样引发的同步问题 还不好解决 为什么不只用一个数组存储消息，把压缩过的摘要作为一条记录存进去打上标记 每次发送给 LLM 的就是过滤掉有压缩标记的不就行了吗 甚至激进一点 可以只加载最近一次压缩的摘要 以及之后的消息

> 可以 实现吧

---

## 收尾

> 在 /Users/lwq/work/person/baseAIlearn/docs 目录下帮我总结今天的开发日志，3.17，今天做了上下文压缩，注意把压缩策略技术选型和思考，以及消息格式设计选型和思考总结进去

> 我发现 readme 还没有更新

> 好了 直接提交吧

> 帮我提交代码

> 我想让你帮我做一件事，现在 docs 是记录开发日志，但是我有一个需求 我想把我所有对你说的话 即单纯就是我说的部分 也整理成一个文档，文件夹名称就叫 developerWords，也是按天来进行整理，由于当前上下文中我已经和你对话很多次了，你就先把我和你实现这个 agent 目前为止 我跟你说的话都整理在 3.17 里面吧
