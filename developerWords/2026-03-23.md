# 2026-03-23 Developer Words

> 记录开发者与 AI 的对话原文（仅开发者侧发言），按话题顺序整理。

---

## 代码沙箱验证方案调研

> 我现在想实现 代码执行沙箱验证（Agent 写完代码后自动编译/运行验证正确性）  但是我之前没接触过 能不能先跟我看看目前主流的设计方案

---

## 路径安全守卫需求

> 我觉得使用方案一可以 但是我希望对整个agent的一些操作做限制，比如对于删除，迁移文件位置的操作即bash操作做检查 但是如果是当前路径下的就放行  如果是路径当前项目路径之外的 就需要进行ask  询问用户

> Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

---

## execute_command grep 报错排查

> } 📤 命令执行失败: Command failed: sleep 3 && ps aux | grep "npm run dev" | grep -v grep  为什么无法执行这个命令

---

## 收尾

> /daily-record

> 今天是3.23  在docs和developerWords更新
